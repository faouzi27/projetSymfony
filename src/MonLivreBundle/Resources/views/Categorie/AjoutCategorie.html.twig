{% extends "@User/Page_Employer.html.twig" %}

{% block body %}
    {{ form_start(form) }}
    <div class="col-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Ajouter Categorie</h4>
                <p class="card-description">
                </p>
                <div class="form-group">
{{ form_start(form) }}
                    {{  form_errors(form.nomCat) }}

                    Nom: {{ form_widget(form.nomCat, { 'attr': {'class': 'form-control'} }) }}<br>
    <input type="submit" value="Envoyer" class=" btn btn-primary mr-2"/>
{{ form_end(form) }}
</div>
</div>
</div>
</div>


{% endblock %}