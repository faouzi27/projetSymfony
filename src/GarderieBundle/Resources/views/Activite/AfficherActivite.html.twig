{% extends "@User/Page_Employer.html.twig" %}
{% block body %}

    <div class="col-lg-20 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Liste Classe</h4>

                <div class="table-responsive pt-3">
                    <table class="table table-bordered">
                        <thead>
                        <tr>
                            <th>
                                id
                            </th>
                            <th>
                                Type
                            </th>
                            <th>
                                Prix
                            </th>

                            <th>
                                Action
                            </th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for s in act %}

                            <tr>
                                <td>
                                    {{ s.id }}
                                </td>
                                <td>
                                    {{ s.type }}
                                </td>
                                <td>
                                    {{ s.prix }}

                                </td>
                                <td>
                                    <a href="{{ path('Activite_delete',{'id':s.id}) }}">Supprimer</a><br>
                                    <a href="{{ path('Activite_modifie',{'id':s.id}) }}">Modifier</a><br>
                                </td>



                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}